{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "pnpm:i": "pnpm install --shamefully-hoist"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.4"
  }
}
